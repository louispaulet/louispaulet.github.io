# Agents Notes

- Enforce sharp corners across the entire site: no element, component, or asset should introduce rounded corners or border radii. Keep every edge at 90°.
- When adding styles or components, override any library defaults that apply `border-radius` so the no-rounded-corners rule stays intact.
